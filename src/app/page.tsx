import { Workspace } from "@/features/study-session/components/Workspace";

/**
 * The route owns nothing. All composition lives in the feature.
 */
export default function HomePage() {
  return <Workspace />;
}
